<h2>Ideas</h2>
<div class="clickable-container">
	<% @ideas.each do |idea| %>
		<a href="/ideas/<%=idea.id%>">
			<div class="idea-container">
				<%= idea.name %>: <%= idea.description %>
			</div>
		</a>
	<% end %>
</div>
<%= will_paginate @ideas %>